# This file is auto-generated by generate_tools.py. DO NOT EDIT.
import logging
from pydantic import ValidationError
from multiconn_archicad.basic_types import Port
from tapir_archicad_mcp.context import multi_conn_instance
from tapir_archicad_mcp.tools.tool_registry import register_tool_for_dispatch
from tapir_archicad_mcp.tools.validation import validate_result, extract_archicad_errors
from multiconn_archicad.models.tapir.commands import (
    CreateKeynoteFoldersParameters,
    CreateKeynoteFoldersResult,
    CreateKeynoteItemsParameters,
    CreateKeynoteItemsResult,
    CreateKeynoteLabelsParameters,
    CreateKeynoteLabelsResult,
    DeleteKeynoteFoldersParameters,
    DeleteKeynoteFoldersResult,
    DeleteKeynoteItemsParameters,
    DeleteKeynoteItemsResult,
    GetKeynoteAutoTextsParameters,
    GetKeynoteAutoTextsResult,
    GetKeynoteTreeResult,
    ModifyKeynoteFoldersParameters,
    ModifyKeynoteFoldersResult,
    ModifyKeynoteItemsParameters,
    ModifyKeynoteItemsResult,
)

log = logging.getLogger()

def create_keynote_folders(port: int, params: CreateKeynoteFoldersParameters) -> CreateKeynoteFoldersResult:
    """
    Creates keynote folders under the given parent folders (or under the root folder). Available from Archicad 28.
    """
    multi_conn = multi_conn_instance.get()
    target_port = Port(port)
    if target_port not in multi_conn.active:
        raise ValueError(f"Port {port} is not an active Archicad connection.")
    conn_header = multi_conn.active[target_port]
    try:

        result_dict = conn_header.core.post_tapir_command(
            command="CreateKeynoteFolders",
            parameters=params.model_dump(mode='json', by_alias=True)
        )
        return validate_result(CreateKeynoteFoldersResult, result_dict)

    except ValidationError as e:
        log.error(f"Validation error for CreateKeynoteFolders result: {e}")
        raise ValueError(extract_archicad_errors(e, "CreateKeynoteFolders"))
    except Exception as e:
        log.error(f"Error executing CreateKeynoteFolders on port {port}: {e}")
        raise e


register_tool_for_dispatch(
    create_keynote_folders,
    name="keynotes_create_keynote_folders",
    title="CreateKeynoteFolders",
    description="Creates keynote folders under the given parent folders (or under the root folder). Available from Archicad 28.",
    params_model=CreateKeynoteFoldersParameters,
    result_model=CreateKeynoteFoldersResult
)


def create_keynote_items(port: int, params: CreateKeynoteItemsParameters) -> CreateKeynoteItemsResult:
    """
    Creates keynote items in the given parent folders (or in the root folder). Available from Archicad 28.
    """
    multi_conn = multi_conn_instance.get()
    target_port = Port(port)
    if target_port not in multi_conn.active:
        raise ValueError(f"Port {port} is not an active Archicad connection.")
    conn_header = multi_conn.active[target_port]
    try:

        result_dict = conn_header.core.post_tapir_command(
            command="CreateKeynoteItems",
            parameters=params.model_dump(mode='json', by_alias=True)
        )
        return validate_result(CreateKeynoteItemsResult, result_dict)

    except ValidationError as e:
        log.error(f"Validation error for CreateKeynoteItems result: {e}")
        raise ValueError(extract_archicad_errors(e, "CreateKeynoteItems"))
    except Exception as e:
        log.error(f"Error executing CreateKeynoteItems on port {port}: {e}")
        raise e


register_tool_for_dispatch(
    create_keynote_items,
    name="keynotes_create_keynote_items",
    title="CreateKeynoteItems",
    description="Creates keynote items in the given parent folders (or in the root folder). Available from Archicad 28.",
    params_model=CreateKeynoteItemsParameters,
    result_model=CreateKeynoteItemsResult
)


def create_keynote_labels(port: int, params: CreateKeynoteLabelsParameters) -> CreateKeynoteLabelsResult:
    """
    Creates Label elements that reference the given keynote items via autotext. Available from Archicad 28.
    """
    multi_conn = multi_conn_instance.get()
    target_port = Port(port)
    if target_port not in multi_conn.active:
        raise ValueError(f"Port {port} is not an active Archicad connection.")
    conn_header = multi_conn.active[target_port]
    try:

        result_dict = conn_header.core.post_tapir_command(
            command="CreateKeynoteLabels",
            parameters=params.model_dump(mode='json', by_alias=True)
        )
        return validate_result(CreateKeynoteLabelsResult, result_dict)

    except ValidationError as e:
        log.error(f"Validation error for CreateKeynoteLabels result: {e}")
        raise ValueError(extract_archicad_errors(e, "CreateKeynoteLabels"))
    except Exception as e:
        log.error(f"Error executing CreateKeynoteLabels on port {port}: {e}")
        raise e


register_tool_for_dispatch(
    create_keynote_labels,
    name="keynotes_create_keynote_labels",
    title="CreateKeynoteLabels",
    description="Creates Label elements that reference the given keynote items via autotext. Available from Archicad 28.",
    params_model=CreateKeynoteLabelsParameters,
    result_model=CreateKeynoteLabelsResult
)


def delete_keynote_folders(port: int, params: DeleteKeynoteFoldersParameters) -> DeleteKeynoteFoldersResult:
    """
    Deletes the given keynote folders including their content. Available from Archicad 28.
    """
    multi_conn = multi_conn_instance.get()
    target_port = Port(port)
    if target_port not in multi_conn.active:
        raise ValueError(f"Port {port} is not an active Archicad connection.")
    conn_header = multi_conn.active[target_port]
    try:

        result_dict = conn_header.core.post_tapir_command(
            command="DeleteKeynoteFolders",
            parameters=params.model_dump(mode='json', by_alias=True)
        )
        return validate_result(DeleteKeynoteFoldersResult, result_dict)

    except ValidationError as e:
        log.error(f"Validation error for DeleteKeynoteFolders result: {e}")
        raise ValueError(extract_archicad_errors(e, "DeleteKeynoteFolders"))
    except Exception as e:
        log.error(f"Error executing DeleteKeynoteFolders on port {port}: {e}")
        raise e


register_tool_for_dispatch(
    delete_keynote_folders,
    name="keynotes_delete_keynote_folders",
    title="DeleteKeynoteFolders",
    description="Deletes the given keynote folders including their content. Available from Archicad 28.",
    params_model=DeleteKeynoteFoldersParameters,
    result_model=DeleteKeynoteFoldersResult
)


def delete_keynote_items(port: int, params: DeleteKeynoteItemsParameters) -> DeleteKeynoteItemsResult:
    """
    Deletes the given keynote items. Available from Archicad 28.
    """
    multi_conn = multi_conn_instance.get()
    target_port = Port(port)
    if target_port not in multi_conn.active:
        raise ValueError(f"Port {port} is not an active Archicad connection.")
    conn_header = multi_conn.active[target_port]
    try:

        result_dict = conn_header.core.post_tapir_command(
            command="DeleteKeynoteItems",
            parameters=params.model_dump(mode='json', by_alias=True)
        )
        return validate_result(DeleteKeynoteItemsResult, result_dict)

    except ValidationError as e:
        log.error(f"Validation error for DeleteKeynoteItems result: {e}")
        raise ValueError(extract_archicad_errors(e, "DeleteKeynoteItems"))
    except Exception as e:
        log.error(f"Error executing DeleteKeynoteItems on port {port}: {e}")
        raise e


register_tool_for_dispatch(
    delete_keynote_items,
    name="keynotes_delete_keynote_items",
    title="DeleteKeynoteItems",
    description="Deletes the given keynote items. Available from Archicad 28.",
    params_model=DeleteKeynoteItemsParameters,
    result_model=DeleteKeynoteItemsResult
)


def get_keynote_auto_texts(port: int, params: GetKeynoteAutoTextsParameters) -> GetKeynoteAutoTextsResult:
    """
    Retrieves the autotext tokens of the given keynote items. The tokens can be used as label text content to reference the fields of a keynote item. Available from Archicad 28.
    """
    multi_conn = multi_conn_instance.get()
    target_port = Port(port)
    if target_port not in multi_conn.active:
        raise ValueError(f"Port {port} is not an active Archicad connection.")
    conn_header = multi_conn.active[target_port]
    try:

        result_dict = conn_header.core.post_tapir_command(
            command="GetKeynoteAutoTexts",
            parameters=params.model_dump(mode='json', by_alias=True)
        )
        return validate_result(GetKeynoteAutoTextsResult, result_dict)

    except ValidationError as e:
        log.error(f"Validation error for GetKeynoteAutoTexts result: {e}")
        raise ValueError(extract_archicad_errors(e, "GetKeynoteAutoTexts"))
    except Exception as e:
        log.error(f"Error executing GetKeynoteAutoTexts on port {port}: {e}")
        raise e


register_tool_for_dispatch(
    get_keynote_auto_texts,
    name="keynotes_get_keynote_auto_texts",
    title="GetKeynoteAutoTexts",
    description="Retrieves the autotext tokens of the given keynote items. The tokens can be used as label text content to reference the fields of a keynote item. Available from Archicad 28.",
    params_model=GetKeynoteAutoTextsParameters,
    result_model=GetKeynoteAutoTextsResult
)


def get_keynote_tree(port: int) -> GetKeynoteTreeResult:
    """
    Retrieves the whole keynote folder and item hierarchy. The technical root folder is not included in the output; the top-level folders and items are returned directly. Available from Archicad 28.
    """
    multi_conn = multi_conn_instance.get()
    target_port = Port(port)
    if target_port not in multi_conn.active:
        raise ValueError(f"Port {port} is not an active Archicad connection.")
    conn_header = multi_conn.active[target_port]
    try:

        result_dict = conn_header.core.post_tapir_command(
            command="GetKeynoteTree",
            parameters={}
        )
        return validate_result(GetKeynoteTreeResult, result_dict)

    except ValidationError as e:
        log.error(f"Validation error for GetKeynoteTree result: {e}")
        raise ValueError(extract_archicad_errors(e, "GetKeynoteTree"))
    except Exception as e:
        log.error(f"Error executing GetKeynoteTree on port {port}: {e}")
        raise e


register_tool_for_dispatch(
    get_keynote_tree,
    name="keynotes_get_keynote_tree",
    title="GetKeynoteTree",
    description="Retrieves the whole keynote folder and item hierarchy. The technical root folder is not included in the output; the top-level folders and items are returned directly. Available from Archicad 28.",
    params_model=None,
    result_model=GetKeynoteTreeResult
)


def modify_keynote_folders(port: int, params: ModifyKeynoteFoldersParameters) -> ModifyKeynoteFoldersResult:
    """
    Modifies the key, title or reference of the given keynote folders. Available from Archicad 28.
    """
    multi_conn = multi_conn_instance.get()
    target_port = Port(port)
    if target_port not in multi_conn.active:
        raise ValueError(f"Port {port} is not an active Archicad connection.")
    conn_header = multi_conn.active[target_port]
    try:

        result_dict = conn_header.core.post_tapir_command(
            command="ModifyKeynoteFolders",
            parameters=params.model_dump(mode='json', by_alias=True)
        )
        return validate_result(ModifyKeynoteFoldersResult, result_dict)

    except ValidationError as e:
        log.error(f"Validation error for ModifyKeynoteFolders result: {e}")
        raise ValueError(extract_archicad_errors(e, "ModifyKeynoteFolders"))
    except Exception as e:
        log.error(f"Error executing ModifyKeynoteFolders on port {port}: {e}")
        raise e


register_tool_for_dispatch(
    modify_keynote_folders,
    name="keynotes_modify_keynote_folders",
    title="ModifyKeynoteFolders",
    description="Modifies the key, title or reference of the given keynote folders. Available from Archicad 28.",
    params_model=ModifyKeynoteFoldersParameters,
    result_model=ModifyKeynoteFoldersResult
)


def modify_keynote_items(port: int, params: ModifyKeynoteItemsParameters) -> ModifyKeynoteItemsResult:
    """
    Modifies the key, title, description or reference of the given keynote items. Available from Archicad 28.
    """
    multi_conn = multi_conn_instance.get()
    target_port = Port(port)
    if target_port not in multi_conn.active:
        raise ValueError(f"Port {port} is not an active Archicad connection.")
    conn_header = multi_conn.active[target_port]
    try:

        result_dict = conn_header.core.post_tapir_command(
            command="ModifyKeynoteItems",
            parameters=params.model_dump(mode='json', by_alias=True)
        )
        return validate_result(ModifyKeynoteItemsResult, result_dict)

    except ValidationError as e:
        log.error(f"Validation error for ModifyKeynoteItems result: {e}")
        raise ValueError(extract_archicad_errors(e, "ModifyKeynoteItems"))
    except Exception as e:
        log.error(f"Error executing ModifyKeynoteItems on port {port}: {e}")
        raise e


register_tool_for_dispatch(
    modify_keynote_items,
    name="keynotes_modify_keynote_items",
    title="ModifyKeynoteItems",
    description="Modifies the key, title, description or reference of the given keynote items. Available from Archicad 28.",
    params_model=ModifyKeynoteItemsParameters,
    result_model=ModifyKeynoteItemsResult
)
